name: Branch Protection

# Ensure newer runs cancel older ones for the same branch
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# Optimized CI: Pre-push hooks validate locally (type-check + tests + lint)
# CI runs only on PR (final gate) and main push (safety net)

on:
  pull_request:
    branches: [main]
    paths-ignore: ['docs/**', 'design/**']
  push:
    branches: [main]  # Safety net for main branch only
    paths-ignore: ['docs/**', 'design/**']

jobs:
  # Gate job that depends on all required checks (PRs only)
  protection-gate:
    name: All checks must pass
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    needs: [type-check, code-quality, test]
    steps:
      - name: All required checks passed
        run: echo "All required checks passed. Safe to merge."

  type-check:
    name: TypeScript Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
      - run: npm ci
      - run: npm run type-check

  code-quality:
    name: Code Quality (ESLint + Prettier)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
      - run: npm ci
      - run: npm run lint
      - run: npm run format:check

  test:
    name: Jest Tests
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
      - run: npm ci
      - run: npm test -- --coverage
      - name: Upload coverage
        uses: codecov/codecov-action@v3
        if: always()
        with:
          files: ./coverage/lcov.info
