# ai rules

- treat /spec as source of truth
- do not invent new features, endpoints, or fields
- prefer small, reviewable diffs
- always add/update tests for changed behavior
- never bypass privacy/auth checks
- if uncertain, leave TODO and write a failing test describing expected behavior
